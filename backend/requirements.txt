# ScholarSync - RAG-based Academic Writing Assistant
# Backend dependencies

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9   # for file uploads

# PDF parsing
PyMuPDF==1.24.5           # imported as `fitz`

# Embeddings (runs locally, no API key needed)
sentence-transformers==3.0.1

# Vector store
chromadb==0.5.3

# LLM API
groq==0.9.0               # Groq Python SDK

# Utilities
python-dotenv==1.0.1      # load .env files
pydantic==2.7.4           # data validation (FastAPI dependency)
